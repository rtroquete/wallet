server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    metrics:
      enabled: true

springdoc:
  swagger-ui:
    path: /docs
    enable: true

spring:
  application:
    name: wallet
#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
